# User-Edited Examples

Store examples here only when the user rewrites Communication Rob's generated answer into a preferred final version and explicitly asks to save it.

Do not store the user's initial draft, rough intent, screenshots, or source context here. Those are task inputs, not user-edited preferred expressions.

## Entry Format

Use this compact format for new entries:

```text
Scene:
Intent:
Expression:
Why it works for the user:
```

## Saved Examples

No user-edited examples saved yet.
